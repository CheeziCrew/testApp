name: Call CodeQL

on:
  push:

jobs:
  call-echo-workflow:
    uses: cheezi0747/testConfig/.github/workflows/codeql.yml@main
    permissions:
      actions: read
      contents: read
      security-events: write
