name: Call CodeQL

on:
  push:

jobs:
  call-echo-workflow:
    uses: CheeziCrew/testConfig/.github/workflows/java-codeql.yml@main
    permissions:
      actions: read
      contents: read
      security-events: write
